# Consistent whitespace across editors. Markdown is the primary format here.
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

[*.md]
# Two trailing spaces are a hard line break in Markdown; do not strip them.
trim_trailing_whitespace = false
max_line_length = 90

[*.{json,yml,yaml,mjs,js,html}]
indent_size = 2
